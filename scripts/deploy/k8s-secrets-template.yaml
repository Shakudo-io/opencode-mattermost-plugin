apiVersion: v1
kind: Secret
metadata:
  name: opencode-teams-bot-credentials
  namespace: hyperplane-pipelines
type: Opaque
stringData:
  AZURE_APP_ID: "REPLACE_WITH_APP_ID"
  AZURE_APP_PASSWORD: "REPLACE_WITH_APP_PASSWORD"
  AZURE_TENANT_ID: "REPLACE_WITH_TENANT_ID"
  AZURE_AD_AUTHORIZED_GROUP_ID: "REPLACE_WITH_GROUP_ID"
